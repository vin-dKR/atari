import React from 'react'
import { ProjectFormTemplate } from '../ProjectFormTemplate'

export const CFLDBudgetUtilization: React.FC = () => {
    return (
        <ProjectFormTemplate
            title="CFLD - Budget Utilization"
            description="Track budget utilization for CFLD program"
        />
    )
}
