import React from 'react'
import { ProjectFormTemplate } from '../ProjectFormTemplate'

export const CFLDExtensionActivity: React.FC = () => {
    return (
        <ProjectFormTemplate
            title="CFLD - Extension Activity"
            description="Manage extension activities for CFLD program"
        />
    )
}
