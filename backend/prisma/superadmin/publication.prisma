// Publication - models only; generator/datasource in schema.prisma

model Publication {
  publicationId   Int    @id @default(autoincrement()) @map("publication_id")
  publicationName String @map("publication_name")
  
  @@map("publication")
  @@index([publicationId])
  @@index([publicationName])
}
